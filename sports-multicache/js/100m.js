(()=>{const $=id=>document.getElementById(id),timer=$("timer"),dist=$("distance"),runner=$("runner"),msg=$("message"),result=$("result"),finalTime=$("final-time"),scoreEl=$("score"),controls=$("controls"),again=$("again"),fill=$("pace-fill"),paceText=$("pace-text");
let state="ready",last=null,start=0,lastPress=0,distance=0,good=0,total=0,intervalId;const OPT_MIN=110,OPT_MAX=210,TOO_FAST=65,STEP=1.85;
const fmt=ms=>(ms/1000).toFixed(2).padStart(5,"0");
function reset(){state="ready";last=null;start=0;lastPress=0;distance=0;good=0;total=0;clearInterval(intervalId);timer.textContent="00.00";dist.textContent="0 M";runner.style.left="3%";msg.textContent="READY?";result.classList.add("hidden");controls.classList.remove("hidden");fill.style.width="0%";paceText.textContent="PRESS A TO START"}
function finish(){state="finished";clearInterval(intervalId);const ms=performance.now()-start;timer.textContent=fmt(ms);finalTime.textContent=fmt(ms);const score=Math.max(0,Math.round(100000-(ms/1000)*7000+good*100));scoreEl.textContent="SCORE "+String(score).padStart(5,"0");msg.textContent="FINISH!";controls.classList.add("hidden");result.classList.remove("hidden");paceText.textContent="RHYTHM "+Math.round(good/Math.max(1,total)*100)+"%"}
function press(b){if(state==="finished")return;if(state==="ready"){if(b!=="A"){msg.textContent="START WITH A!";return}state="running";start=performance.now();lastPress=start;last="A";msg.textContent="RUN!";paceText.textContent="ALTERNATE A / B";intervalId=setInterval(()=>timer.textContent=fmt(performance.now()-start),20);return}if(b===last){msg.textContent="ALTERNATE!";return}const now=performance.now(),dt=now-lastPress;lastPress=now;last=b;total++;
if(dt<TOO_FAST){distance=Math.max(0,distance-.8);msg.textContent="TOO FAST!";paceText.textContent="KEEP YOUR RHYTHM"}else if(dt>=OPT_MIN&&dt<=OPT_MAX){distance+=STEP;good++;msg.textContent="GOOD!";paceText.textContent="PERFECT PACE"}else{distance+=STEP*.72;msg.textContent=dt<OPT_MIN?"SLOW DOWN!":"FASTER!";paceText.textContent="FIND THE RHYTHM"}
fill.style.width=Math.min(100,good/Math.max(1,total)*100)+"%";runner.style.left=Math.min(92,3+distance/100*89)+"%";dist.textContent=Math.min(100,Math.round(distance))+" M";if(distance>=100)finish()}
ArcadeController.on("A",()=>press("A"));ArcadeController.on("B",()=>press("B"));again.addEventListener("click",reset);reset()})();